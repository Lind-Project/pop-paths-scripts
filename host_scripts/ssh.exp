#!/usr/bin/expect

# logging into ssh

# set index [lindex $argv 0];
set index 1;

spawn bash ssh_command.sh $index
expect {
    "yes/no" {
        send "yes\r"
        expect "*?assword" {
            send "password\r"
        }
    } 
    
    "*?assword" {
        send "password\r"
    }

}