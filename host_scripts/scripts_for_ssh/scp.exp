#!/usr/bin/expect -d

spawn sudo scp -P 2222 -r 'ubuntu@localhost:/home/ubuntu/GCOV_DATA' /hdd/GCOV_DATA

expect {
    "yes/no" {
        send "yes\r"
        expect "*?assword" {send "password\r"}
    } 
    
    "*?assword" {send "password\r"}
}

interact