#!/usr/bin/expect -f

spawn sudo scp -P 2222 -r 'ubuntu@localhost:/home/ubuntu/GCOV_DATA' /hdd/GCOV_DATA/$(date --iso-8601="minutes")
expect {
    -re ".*es.*o.*" {
        exp_send "yes\r"
        exp_continue
    }   
    -re ".*sword.*" {
        exp_send "password\r"
    }
}

exit